class region(int id, string name)
	hat variablen: 
		int id
		string name
		float bevoelkerungsdichte
		list(namedTupel) Zeitreihe
		list(namedTupel) Maﬂname
	
	functions:
		get_id()
		get_name()
		get_infectionNumber(datetime)
		set_zeitreihe(list(namedTupel))
		set_massname(list(namedTupel))

class model()
	Implementierung verschiedener Vorhersagemodelle
	Nutzt object region

class dataParser()

class main()